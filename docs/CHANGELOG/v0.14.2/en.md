# Open Design 0.14.2

## Improved

- Added stable release-note assets for packaged updater metadata.
- Added updater fallback loading for localized HTML and Markdown release notes.

## Fixed

- Stable release validation now fails when required release-note Markdown files are missing.
